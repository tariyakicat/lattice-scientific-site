Put collected client, university, journal, and institute logo files in this folder.

Preferred formats:
- SVG for clean vector logos
- PNG with transparent background
- WebP for raster-only logos

When the files are ready, update `clients[].logo` in `src/data/site.js` to paths like:

```js
logo: "/client-logos/kaust.svg"
```
